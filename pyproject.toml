[project]
name = "travelkeet-bot"
version = "0.1.0"
description = "TravelKeet AI trip planner agent"
requires-python = ">=3.11"
readme = "README.md"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

